# AuditCraft v2 Backend Dependencies
# Python 3.10+

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0

# Data validation
pydantic==2.9.0

# Solidity compilation
py-solc-x==2.0.3

# AST analysis (optional but recommended)
# Slither is heavy; include if available, fallback to regex+AST
# slither-analyzer==0.10.4  # Uncomment if installed

# HTTP client for external calls
httpx==0.27.0

# Utilities
python-multipart==0.0.12  # For file uploads
